meta {
  name: Login Admin
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@gmail.com",
    "password": "Budilibaba123%"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
